{
    "provider": "azure",
    "credential": {
        "clientId": "${azure_client_id}",
        "clientSecret": "${azure_client_secret}",
        "tenantId": "${azure_tenant_id}",
        "subscruptionId": "${azure_subscription_id}"
    }
}